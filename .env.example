# Wix Headless OAuth client id (publishable, read-only for public content).
# Create it in the Wix dashboard: Settings → Headless settings → create an
# OAuth app, then copy the Client ID here into a real `.env` file.
# This does NOT affect or unpublish your live Wix site.
VITE_WIX_CLIENT_ID=
